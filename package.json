{
  "name": "nftyfinance-monorepo",
  "version": "0.0.1",
  "private": "true",
  "scripts": {
    "contracts": "yarn workspace nftyfinance-contracts",
    "dapp": "yarn workspace nftyfinance-dapp",
    "faucet": "yarn workspace nftyfinance-faucet",
    "subgraph": "yarn workspace nftyfinance-subgraph",
    "dapp-v1": "yarn workspace nftyfinance-dapp-v1",
    "graph-node": "cd ../graph-node/docker && rm -rf data && docker compose down && docker compose up",
    "deploy:local": "yarn contracts deploy:local && yarn subgraph deploy:local",
    "build:dapp": "yarn contracts build && yarn dapp build:prod",
    "build:faucet": "yarn contracts build && yarn faucet build"
  },
  "workspaces": [
    "contracts",
    "dapp",
    "faucet",
    "subgraph",
    "dapp-v1"
  ]
}
