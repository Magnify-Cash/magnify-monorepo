{
  "name": "nftyfinance-subgraph",
  "version": "0.0.1",
  "scripts": {
    "codegen": "graph codegen",
    "deploy:testnet": "graph codegen && graph deploy --product hosted-service skulltech/nftyfinance-sepolia --network sepolia",
    "deploy:local": "graph codegen && graph remove --node http://localhost:8020/ nftyfinance-local && graph create --node http://localhost:8020/ nftyfinance-local && graph deploy -l 0.0.1 --node http://localhost:8020/ --ipfs http://localhost:5001 nftyfinance-local --network mainnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
